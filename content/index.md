
<!-- This is the metadata structure for all doc files
title:                          # required
section:                        # required — matches URL segment
slug:                           # required — stable across versions
since:                          # required — when this page was introduced
until:                          # optional — omit if still current
changed_in:                     # optional — only present if variants exist
  v1.2:                         # key: version, value: slug of variant file
tags: []                        # optional — for search/filtering later
-->

# Docs Home Page